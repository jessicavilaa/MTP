#include<stdio.h>
#include<stdlib.h>
#include<ctype.h>
#define NCHAR 256
int main(){
int i;
char frase[NCHAR];
printf("Digite uma frase:");
fgets(frase, NCHAR, stdin);
for(i=0;frase[i];i++)
    (endereco[strlen(endereco)-1] == 10)
       endereco[strlen(endereco)-1] = '\0';
printf("A frase '%s' tem %d caracteres\n", s, strlen(s));
return EXIT_SUCCESS;
}
